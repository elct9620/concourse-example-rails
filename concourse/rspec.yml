---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: ruby
    tag: '2.3-alpine'

inputs:
- name: rails

run:
  path: sh
  args:
    - -exc
    - |
      cd rails
      bundle install
      bundle exec rspec
